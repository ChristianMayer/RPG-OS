# include/rpgos

Public headers of the `rpgos` library live in this directory, one header per
module, e.g. `include/rpgos/engine.hpp`.

Everything that a user of the library should see goes here; internal helpers
belong in `src/` instead.
