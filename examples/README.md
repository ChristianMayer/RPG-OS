# examples

Small, self-contained programs that demonstrate how to use the `rpgos` library.
Each example lives in its own subdirectory with its own source file(s) and is
registered as an executable in the build.

Not part of the library itself and not installed.
